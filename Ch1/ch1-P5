# Total number of possible routes to be taken by the Traveling
# Salesman problem

cities = int(input('How many cities? '))

import math
result = math.factorial(cities)
print('For', cities, 'cities, there are', result, 'possible routes.')
